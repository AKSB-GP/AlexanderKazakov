---

---

<style>
    .circle-container {
        position: relative;
        width: 600px; 
        height: 600px; 
        list-style: none;
        border-radius: 50%;
        /*border-left:2px solid white;*/
    }
    .navcircle{
        position:relative;
        width: 600px; 
        height: 600px; 
        border-radius:50%;
        border: 2px solid black;
        border-left: 2px solid white;
        transform: rotate(-8deg);
    }

    .circle-container li {
        position: absolute;
        top: 50%;
        right: 60%;
        transform-origin: center;
        text-align: center;
        transform: rotate(var(--rotate, 10deg)) translate(280px)
            rotate(calc(-1 * var(--rotate, 0deg)));
    }

    .circle-container li h1 {
        margin: 0;
        font-size: 2rem; /* 48px */
        line-height: 1;
    }

    .circle-container li a {
        display: block;
        text-decoration: none;
        color: white;
    }




    /*
relative w-[300px] h-[300px] list-none p-0 m-0 border border-black rounded-full


*/
</style>
<ul class="circle-container">
    <div class="navcircle"></div>
    <li>
        <h1><a href="/Work">WORK</a></h1>
    </li>
    <li>
        <h1><a href="/About">ABOUT</a></h1>
    </li>

    <li>
        <h1><a href="/Projects">PROJECTS</a></h1>
    </li>
    <li>
        <h1><a href="/Contact">CONTACT</a></h1>
    </li>

</ul>
<script>
    document.addEventListener("DOMContentLoaded", function () {
        const items = document.querySelectorAll(".circle-container li");
        const totalItems = items.length;
        const angleStep = 90 / (totalItems - 1); // Spread items over 180 degrees

        items.forEach((item, index) => {
            const angle = angleStep * index + 130; // Adjust to start from top center
            (item as HTMLElement).style.setProperty("--rotate", `${angle}deg`);
        });
    });
</script>